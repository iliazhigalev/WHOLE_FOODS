[project]
name = "whole-foods"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.122.0",
    "loguru>=0.7.3",
    "uvicorn[standard]>=0.38.0",
]

[dependency-groups]
dev = [
    "bandit>=1.9.2",
    "mypy>=1.18.2",
    "pre-commit>=4.5.0",
    "radon>=6.0.1",
    "ruff>=0.14.6",
]
